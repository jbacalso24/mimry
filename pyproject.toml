[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mimry"
version = "0.1.0"
description = "Modular Intelligence Memory for Repos"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "fastmcp>=3.4.2",
    "graphifyy @ git+https://github.com/safishamsi/graphify.git@44c0a5e33c7011813dcebf1a8850c1c6005bf500",
    "tree-sitter>=0.25.2",
    # 1.13.x changes the TSX grammar shape and breaks exported function/component
    # extraction. Keep the version proved by the locked and unlocked artifact tests.
    "tree-sitter-language-pack==1.12.2",
]

[project.scripts]
mimry = "mimry.cli:main"
mimry-mcp = "mimry.mcp_server:main"

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=8",
    "ruff>=0.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/mimry"]

# Keep source releases reviewable. Generated state, local work products, and
# the optional Graphify submodule are intentionally omitted.
[tool.hatch.build.targets.sdist]
include = [
    "/src/mimry",
    "/tests",
    "/docs",
    "/assets/banner.svg",
    "/README.md",
    "/CHANGELOG.md",
    "/RELEASING.md",
    "/THIRD_PARTY.md",
    "/LICENSE",
    "/pyproject.toml",
]
exclude = ["/tests/fixtures/**/.env"]

[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = ["vendor"]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
